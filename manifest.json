{
	"manifest_version": 2,
	"name": "Mockify Text",
	"version": "1.0",
	"description": "Adds a context menu when text in input fields are selected to allow turning them into SpOngEbOB MocKIng TeXT.",

	"background": {
		"scripts": ["src/background.js", "src/mockify.js"]
	},

	"permissions": [
		"activeTab",
		"menus"
	]
}
